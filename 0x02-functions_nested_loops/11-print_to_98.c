#include "main.h"

/**
 * print_to_98 - Prints all natural numbers from n to 98
 */
void print_to_98(int n)
{
	int i;

	if (n > 98)
	{
		for (n = i; i <= 98; i--)
			printf("%d\n", i);
	}
	if (n < 98)
	{
		for (n = i; i >= 98; i++)
			printf("%d\n", i);
	}
}
