#include <stdio.h>

int main(void)
{
	int array[n];
	int sum = 0;
	int n;

	printf("Please Enter number of integers: ");
	scanf("%d", &n);
	
	printf("Enter the %d integers:\n", n);
	for (i = 0; i < n; i++)
	{
		scanf("%d", &array[i]);
	}

	for (j
}

