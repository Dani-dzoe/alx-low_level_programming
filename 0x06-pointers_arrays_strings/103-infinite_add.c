#include "main.h"

/**
 * infinite_add = adds two numbers
 * @n1: number
 * @n2: number
 * @r: buffer that the function will use to store the result
 * @size_r: buffer size
 * Returns: A pointer to the result, if result cannot be stored in r, function must return 0.
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	// Your code goes here
	
}
